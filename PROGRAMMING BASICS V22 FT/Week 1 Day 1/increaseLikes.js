// Initialize the variable likeCount with value 0
var likeCount =0;
// function to increase the number of likes
function increaselikes(){
    // increment the variable likeCounte by 1 
    likeCount = likeCount + 1;
}